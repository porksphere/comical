node_modules/
dist/
*.tsbuildinfo
.DS_Store
bun.lock
*.log
coverage/
demo/.out/
.cache/
.comical/
**/.tmp-*/
.comical-demo/
# Local runtime/sync data (device tokens, synced library snapshots) — never committed; it once
# leaked bridge data into an unpushed commit that had to be rewritten out.
.comical-sync/

# Local Claude Code state (worktrees, settings, memory) — never committed
.claude/

# Playwright e2e capture lane (on-demand recordings/screenshots — never committed)
e2e/.test-results/
e2e/.captures/
playwright-report/
test-results/

# Generated native runtime bundles — built by `bun run build:native` (and in CI), never committed.
packages/host-ios/Sources/ComicalHostIOS/Resources/harness.js
packages/host-android/src/main/assets/comical_harness.js

# Gradle / Android build artifacts
.gradle/
build/
**/build/
local.properties
.kotlin/

# Isolated dev sandbox — bridges targeting specific real sites are NEVER committed
# (preserves the content-neutral posture). Build/test them here only.
sandbox/
