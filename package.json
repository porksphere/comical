{
  "name": "comical",
  "version": "0.0.0",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "description": "Configurable, content-neutral bridge runtime for serialized content",
  "engines": {
    "bun": ">=1.3.0"
  },
  "private": true,
  "scripts": {
    "build": "bun run scripts/build.ts",
    "build:native": "bun run scripts/build-native-runtime.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "cli": "bun run packages/cli/src/index.ts",
    "demo:server": "bun --watch run demo/demo-server.ts",
    "demo:dev": "bun run demo/serve.ts",
    "serve": "bun run packages/host-server/src/server.ts"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "bridges/*"
  ],
  "dependencies": {
    "lucide": "^1.17.0"
  }
}
